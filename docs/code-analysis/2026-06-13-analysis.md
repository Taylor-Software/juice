# Code Analysis — 2026-06-13

**Project:** Juice Oracle (Flutter solo-TTRPG companion)
**Scope:** 44 source files, 13,605 LOC; 61 test files, 10,287 LOC; 619 tests
**Toolchain:** Dart/Flutter, `flutter_lints` + `strict-casts`/`strict-raw-types`

## Headline

Healthy codebase: 619 tests green, 85% line coverage, `flutter analyze` at a
single long-standing info, no security issues, no dead dependencies, no
TODOs. Two real latent bugs (a negative-modulo `RangeError` in the NPC dialog
walk and an unguarded force-unwrap) plus a crop of DRY/dead-code cleanups.
The one structural concern is `journal_screen.dart` (1,826 LOC) absorbing
the whole cycle-4 build — a god object with clean extraction seams.

Top-3 priorities: (1) fix the dialog `%5` crash + the `commandById!` guard;
(2) clear the DRY/dead-code crop and the lint baseline; (3) split
`journal_screen.dart` as a separate focused refactor.

## Findings

### 🔴 Critical
- _(none — the scanner's `%5` RangeError was a **FALSE POSITIVE**: Dart's `%`
  returns non-negative for a positive divisor (`-1 % 5 == 4`), unlike C/Java,
  so the dialog-grid walk already wraps correctly. Left the original code;
  added a regression test (`fate_engine_test.dart`, "walking off a grid edge
  wraps") + a clarifying comment to stop the next reviewer re-flagging it.)_

### 🟠 High
- **`lib/features/journal_screen.dart:320`** — `commandById(…, _fateCommandId(ora))!`
  unguarded force-unwrap; a future registry typo panics. **Fix:** null-guard
  return. ✅ FIXED
- **`lib/features/journal_screen.dart`** (1,826 LOC) — god object: list UI +
  composer + slash/mention/ask + 4 dialogs + interpret/voice/recap/export +
  `_CampaignHeader`. Clean seams: `journal/dialogs.dart` (~220), entry
  rendering (~170), composer (~350). **Deferred** — separate refactor PR to
  keep review tractable and protect the 87% coverage (see Top moves).

### 🟡 Medium
- **`oracle.dart:614`** — `mythicMeaning.firstWhere((t)=>t['id']==id)` no
  `orElse`; a stale id throws. (Sibling d100/d6 band `firstWhere`s at 589/642/647
  are safe — last band covers the max roll.) **Fix:** `orElse` with a clear
  `ArgumentError`. ✅ FIXED
- **`oracle_interpreter.dart:360`** — `parseSummary` used a weaker `<think>`
  regex than `_stripThink` (missed unterminated spans). **Fix:** call
  `_stripThink`. ✅ FIXED
- **slug helper duplicated** — `journal_screen.dart` + `home_shell.dart` both
  inline the same two-line slugify. **Fix:** shared `slugify()` in
  `journal_export.dart`. ✅ FIXED
- **`oracle_interpreter.dart:109/297`** — `flat()`/`orElse()` nested helpers
  duplicated across `buildOraclePrompt`/`buildVoicePrompt`. **Fix:** hoist to
  module-level. ✅ FIXED
- **odds mapping duplicated** — `_oddsOptions` in `journal_screen` and
  `_SlashRowState` switch the same oracle/command → odds ladder. **Fix:**
  `oddsForCommandSystem()` in `command_registry.dart`. ✅ FIXED
- **emulation helpers duplicated** — `_freshCharacter`/`_currentEmulation`/
  `_updateEmulation` copy-pasted between `party_emulator_screen` and
  `sidekick_screen` (~32 LOC). **Deferred** — a `CharacterEmulationMixin` is
  the right fix but touches two large stateful screens; lower-risk as its own
  change. Noted.

### 🟢 Low
- **`interpreter.dart:77`** — `runInterpreterEval` has no call site. **Kept** —
  intentional debug-eval harness (spec "Quality bar"), consumes `kEvalSeeds`;
  deleting removes a documented capability for no real gain.
- **`result_card.dart:54`** — `RollRow` public but only used in-file. **Fix:**
  privatize `_RollRow`. ✅ FIXED
- **`models.dart:2`** — dangling library doc comment (the lone analyzer info).
  **Fix:** add `library;`. ✅ FIXED — analyzer now at **0 issues**.
- **`providers.dart:715/772/805`** — `catch (_)` silently resets persisted
  state (MRU/dismissed/recap). Acceptable for cache, but undiagnosable.
  **Deferred** — benign; a debug-only log would help. Noted.

### 💡 Ideas
- `enum OracleId { juice, mythic, rollHigh }` instead of bare strings in 12
  spots — exhaustiveness checking. Churn vs. payoff: deferred.
- `sharedPrefsProvider` to inject one `SharedPreferences` instead of 24
  `getInstance()` calls — no perf bug today (plugin caches). Deferred.
- Typed `JournalPayload.fromMap` wrapper over the raw `payload` string keys.
  Deferred.
- Legacy `juice.log.v1` migration (`providers.dart:66`, `campaign_io.dart:71`)
  and un-suffixed-key migration (`providers.dart:577`) — **keep** (removing
  risks old-install data loss); revisit behind a version gate.
- Memoize `buildCommandRegistry()` (rebuilt per keystroke in the palette).
  Micro; deferred.

## Coverage gaps

- `lib/features/moves_screen.dart` — 0% (155 lines): Datasworn moves browser,
  untested. Worth a smoke test.
- `lib/features/tables_screen.dart` — 0% (48 lines): table browser, untested.
- `lib/state/interpreter_gemma.dart` — 14% (expected; never constructed in
  tests by design — the fake is used everywhere).
- Overall 85% (5138/6045) — healthy.

## Top 3 next moves

1. **Split `journal_screen.dart`** into `journal/{dialogs,entry_rendering,
   composer}.dart` — mechanical, seams identified, tests guard it. Own PR.
2. **Smoke-test `moves_screen` + `tables_screen`** to lift the two 0% files.
3. **`CharacterEmulationMixin`** to de-dup the party/sidekick emulation block.

## Methodology

Generated by `/code-analysis` (lint + coverage + two parallel scanners +
inline perf/dispose/security pass). ✅-marked findings were fixed in the same
pass per the user's request; deferred items are large refactors or
data-risk/product decisions, called out above. Re-run after the journal_screen
split.
